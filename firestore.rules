rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // User documents: only accessible directly by the authenticated owner
    // Backend functions use Firebase Admin SDK which bypasses security rules
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
